# Security

Report vulnerabilities by opening a private advisory or contacting the project
maintainers through the repository security channel.

Generated skills are treated as untrusted code and instructions. openskill-kit
blocks critical scanner findings by default, creates backups on overwrite, and
does not read `.env` files or print secret values during doctor checks.

Supported versions: current `main` and latest published release once releases
exist.
