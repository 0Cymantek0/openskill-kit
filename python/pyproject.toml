[project]
name = "openskillkit-evolution"
version = "0.1.0"
description = "Local OpenWorld evolution engine scaffold for OpenSkillKit."
requires-python = ">=3.10"

[project.scripts]
openskillkit-evolution = "openskillkit_evolution.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
